const DEFAULT_PRODUCTS = [
  {name:'Clásica Smash',price:330,cat:'Hamburguesas',desc:'Pan tortuga · medallón smash · cheddar · cebolla picada · tomate · salsa Maléfica',img:'clasica-smash.jpg'},
  {name:'Doble Impacto',price:530,cat:'Hamburguesas',desc:'Tortuga de queso · doble medallón · extra cheddar · panceta · cebolla caramelizada · lechuga · tomate · salsas a elección',img:'doble-impacto.jpg'},
  {name:'Smash Picante',price:390,cat:'Hamburguesas',desc:'Tortuga de papa · doble smash · extra cheddar · panceta · cebolla caramelizada · tomate · mayonesa · ketchup · Tabasco · salsa Maléfica',img:'smash-picante.jpg'},
  {name:'Smash Barbacoa',price:390,cat:'Hamburguesas',desc:'Tortuga de papa · doble smash · extra cheddar · panceta · lechuga · tomate · salsa Maléfica · barbacoa',img:'smash-barbacoa.jpg'},
  {name:'Smash Crocante',price:390,cat:'Hamburguesas',desc:'Tortuga de papa · doble smash · extra cheddar · panceta · lechuga · tomate · mayonesa · ketchup · Tabasco · salsa Maléfica',img:'smash-crocante.jpg'},
  {name:'Smash Intensa',price:390,cat:'Hamburguesas',desc:'Tortuga de papa · doble smash · extra cheddar · panceta · cebolla caramelizada · lechuga · tomate · barbacoa · Tabasco · mayonesa',img:'smash-intensa.jpg'},
  {name:'Hechizo de Queso',price:430,cat:'Hamburguesas',desc:'Tortuga de queso · medallón de carne · extra cheddar · panceta · cebolla caramelizada · salsa Maléfica · provolone',img:'hechizo-de-queso.jpg'},
  {name:'Mini Smash',price:270,cat:'Hamburguesas',desc:'Pan tortuga · medallón smash · extra cheddar · mayonesa · ketchup',img:'mini-smash.jpg'},
  {name:'Combo Apertura',price:290,cat:'Combos',desc:'Pan tortuga · medallón smash · cheddar · salsa Maléfica · papas fritas',img:'combo-apertura.jpg'},
  {name:'Combo Junior',price:350,cat:'Combos',desc:'Mini Smash · papas fritas · jugo',img:'combo-junior.jpg'},
  {name:'Papas rústicas caseras para compartir',price:360,cat:'Papas',desc:'Papas rústicas en gajos · cheddar · panceta',img:'papas-rusticas-caseras-para-compartir.jpg'},
  {name:'Porción de papas fritas',price:65,cat:'Papas',desc:'Papas fritas',img:'porcion-de-papas-fritas.jpg'},
  {name:'Porción XXL',price:95,cat:'Papas',desc:'Papas fritas XXL',img:'porcion-xxl.jpg'},
  {name:'Aros de cebolla',price:95,cat:'Entradas',desc:'Entrada de aros de cebolla',img:'aros-de-cebolla.jpg'},
  {name:'Agua 500 ml',price:0,cat:'Bebidas',desc:'',emoji:'💧'}, {name:'Agua 2 L',price:0,cat:'Bebidas',desc:'',emoji:'💧'},
  {name:'Coca-Cola 600 cc',price:0,cat:'Bebidas',desc:'',emoji:'🥤'}, {name:'Coca-Cola 1.5 L',price:0,cat:'Bebidas',desc:'',emoji:'🥤'},
  {name:'Cerveza Stella',price:0,cat:'Bebidas',desc:'',emoji:'🍺'}, {name:'Cerveza Zilertal',price:0,cat:'Bebidas',desc:'',emoji:'🍺'},
  {name:'Cerveza Mahou',price:0,cat:'Bebidas',desc:'',emoji:'🍺'}, {name:'Cerveza Budweiser',price:0,cat:'Bebidas',desc:'',emoji:'🍺'}
];
const EXPENSE_CATEGORIES=['Bebida','Cerveza','Refresco','Agua','Harina','Carne','Queso','Cheddar','Panceta','Alquiler','Luz','Agua/Lavado','POS','Otros'];
const DEFAULT_EXTRAS=[{name:'Extra carne',price:120},{name:'Extra queso',price:60},{name:'Extra cheddar',price:70},{name:'Extra panceta',price:90}];
const STORE={products:'master_products_v2',stock:'master_stock',sales:'master_sales',pending:'master_pending',expenses:'master_expenses',expenseCategories:'master_expense_categories',extras:'master_burger_extras',next:'master_next_order'};
const load=(k,f)=>{try{const v=localStorage.getItem(k);return v===null?f:JSON.parse(v)}catch{return f}};
let products=load(STORE.products,DEFAULT_PRODUCTS), stock=load(STORE.stock,[]), sales=load(STORE.sales,[]), pending=load(STORE.pending,[]), expenses=load(STORE.expenses,[]), expenseCategories=load(STORE.expenseCategories,EXPENSE_CATEGORIES), extras=load(STORE.extras,DEFAULT_EXTRAS), nextOrder=Number(localStorage.getItem(STORE.next)||1), cart=[];
let activeCategory='Todos',stockFilter='all',openSaleId=null;
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const money=n=>'$'+Number(n||0).toLocaleString('es-UY');
const dayKey=d=>new Date(d).toISOString().slice(0,10);
const today=()=>new Date().toISOString().slice(0,10);
const escapeHTML=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function normalizeStock(){const map=new Map(stock.map(x=>[x.name,x]));products.forEach(p=>{if(!map.has(p.name))stock.push({name:p.name,qty:20,min:5,cost:0})});save()}
function save(){localStorage.setItem(STORE.products,JSON.stringify(products));localStorage.setItem(STORE.stock,JSON.stringify(stock));localStorage.setItem(STORE.sales,JSON.stringify(sales));localStorage.setItem(STORE.pending,JSON.stringify(pending));localStorage.setItem(STORE.expenses,JSON.stringify(expenses));localStorage.setItem(STORE.expenseCategories,JSON.stringify(expenseCategories));localStorage.setItem(STORE.extras,JSON.stringify(extras));localStorage.setItem(STORE.next,String(nextOrder))}
function showToast(message){const t=$('#toast');t.textContent=message;t.classList.add('show');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>t.classList.remove('show'),2300)}
function showView(name){$$('.view').forEach(v=>v.classList.toggle('active',v.id===`view-${name}`));$$('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.view===name));if(name==='orders')renderOrders();if(name==='stock')renderStock();if(name==='sales')renderSales();if(name==='expenses')renderExpenses();if(name==='settings')renderSettings();window.scrollTo(0,0)}
function renderCategories(){const cats=['Todos',...new Set(products.map(p=>p.cat))];$('#categoryTabs').innerHTML=cats.map(c=>`<button class="${c===activeCategory?'active':''}" data-cat="${escapeHTML(c)}">${escapeHTML(c)}</button>`).join('')}
function renderProducts(){const q=$('#productSearch').value.trim().toLowerCase();const list=products.filter(p=>(activeCategory==='Todos'||p.cat===activeCategory)&&(!q||`${p.name} ${p.desc}`.toLowerCase().includes(q)));$('#productGrid').innerHTML=list.length?list.map((p)=>{const idx=products.indexOf(p),s=stock.find(x=>x.name===p.name),low=s&&s.qty<=s.min,imgSrc=p.img&&(window.PRODUCT_IMAGES?.[p.img]||`./assets/${p.img}`),qty=cart.filter(x=>x.productIndex===idx).reduce((n,x)=>n+x.qty,0),allowsExtras=['hamburguesas','combos'].includes(String(p.cat).toLowerCase());return `<article class="product-card ${qty?'selected':''}" data-product="${idx}" title="${escapeHTML(p.desc)}" role="button" tabindex="0" aria-label="Agregar ${escapeHTML(p.name)}">${qty&&allowsExtras?`<button type="button" class="card-extra-btn" data-product-extra="${idx}">+ Extras</button>`:''}${imgSrc?`<img class="product-image" src="${imgSrc}" alt="${escapeHTML(p.name)}">`:`<span class="product-placeholder">${p.emoji||'🍔'}</span>`}<span class="product-info"><h3>${escapeHTML(p.name)}</h3><span class="product-bottom"><span><span class="product-price">${p.manualPrice||p.price<=0?'Definir precio':money(p.price)}</span>${s?`<span class="product-stock ${low?'low':''}"> · ${s.qty} disponibles</span>`:''}</span>${qty?`<span class="card-stepper"><button type="button" data-product-action="minus" data-index="${idx}" aria-label="Restar ${escapeHTML(p.name)}">−</button><strong>${qty}</strong><button type="button" data-product-action="plus" data-index="${idx}" aria-label="Sumar ${escapeHTML(p.name)}">+</button></span>`:`<span class="add-mark">+</span>`}</span></span></article>`}).join(''):`<div class="empty-state"><div><div class="empty-icon">⌕</div><p>No encontramos productos</p><small>Probá con otra búsqueda.</small></div></div>`}
function addProduct(index){const p=products[index];if(!p)return;if(p.manualPrice||p.price<=0)return askVariablePrice(index);addCartLine(index,p.price,[])}
function addCartLine(index,basePrice,selectedExtras=[]){const p=products[index],extraTotal=selectedExtras.reduce((sum,x)=>sum+Number(x.price),0),price=Number(basePrice)+extraTotal,key=selectedExtras.map(x=>x.name).sort().join('|'),line=cart.find(x=>x.productIndex===index&&x.price===price&&(x.extrasKey||'')===key);line?line.qty++:cart.push({productIndex:index,name:p.name,price,basePrice:Number(basePrice),extras:selectedExtras,extrasKey:key,qty:1});renderCart()}
function openBurgerExtras(index){const p=products[index],manual=p.manualPrice||p.price<=0;openModal(`<h2>${escapeHTML(p.name)}</h2><form id="burgerExtrasForm" class="modal-form">${manual?'<label>Precio base<input id="burgerBasePrice" type="number" min="1" step="1" required autofocus></label>':''}<div class="extras-picker"><span>Agregar extras</span>${extras.length?extras.map((x,i)=>`<label><input type="checkbox" data-extra-index="${i}"><span>${escapeHTML(x.name)}</span><b>+${money(x.price)}</b></label>`).join(''):'<p class="muted">No hay extras configurados.</p>'}</div><button class="primary-action" type="submit">Agregar al ticket</button></form>`);$('#burgerExtrasForm').onsubmit=e=>{e.preventDefault();const basePrice=manual?Number($('#burgerBasePrice').value):Number(p.price);if(basePrice<=0)return;const selected=[...$$('[data-extra-index]:checked')].map(el=>({...extras[Number(el.dataset.extraIndex)]}));addCartLine(index,basePrice,selected);closeModal()}}
function changeProductQty(index,delta){if(delta>0)return addProduct(index);const lineIndex=cart.findIndex(x=>x.productIndex===index);if(lineIndex<0)return;cart[lineIndex].qty--;if(cart[lineIndex].qty<=0)cart.splice(lineIndex,1);renderCart()}
function askVariablePrice(index){const p=products[index];openModal(`<h2>Precio de ${escapeHTML(p.name)}</h2><form id="priceModalForm" class="modal-form"><label>Precio para este pedido<input id="variablePrice" type="number" min="1" step="1" placeholder="$ 0" autofocus required></label><button class="primary-action" type="submit">Agregar al ticket</button></form>`);$('#priceModalForm').onsubmit=e=>{e.preventDefault();const price=Number($('#variablePrice').value);if(price<=0)return;addCartLine(index,price,[]);closeModal()}}
function cartTotal(){return cart.reduce((sum,x)=>sum+x.price*x.qty,0)}
function renderCart(){const root=$('#cart');root.innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-item"><div class="qty-controls"><button data-cart-action="plus" data-index="${i}" aria-label="Sumar">+</button><strong>${x.qty}</strong><button data-cart-action="minus" data-index="${i}" aria-label="Restar">−</button></div><div><span class="cart-name">${escapeHTML(x.name)}</span>${x.extras?.length?`<span class="cart-extras">${x.extras.map(e=>escapeHTML(e.name)).join(' · ')}</span>`:''}</div><div class="cart-line"><strong>${money(x.price*x.qty)}</strong><button data-cart-action="remove" data-index="${i}" aria-label="Quitar ${escapeHTML(x.name)}" title="Quitar">×</button></div></div>`).join(''):`<div class="empty-state"><div><div class="empty-icon">⌁</div><p>El ticket está vacío</p><small>Tocá un producto para agregarlo.</small></div></div>`;const total=money(cartTotal());$('#cartTotal').textContent=total;$('#actionTotal').textContent=total;$('#saveOrder').disabled=!cart.length;$('#clearCart').disabled=!cart.length;renderProducts()}
function clearOrderForm(){cart=[];$('#customer').value='';$('#notes').value='';$('#paymentConfirmed').checked=false;renderCart()}
function registerOrder(){if(!cart.length)return;const order={id:Date.now(),no:nextOrder++,customer:$('#customer').value.trim(),type:$('#orderType').value,source:$('#source').value,paymentMethod:$('#paymentMethod').value,paymentStatus:$('#paymentConfirmed').checked?'Pagado':'Pendiente',items:cart.map(({name,price,qty,extras})=>({name,price,qty,extras:extras||[]})),total:cartTotal(),obs:$('#notes').value.trim(),date:new Date().toISOString()};sales.push(order);pending.unshift(order);order.items.forEach(item=>{const s=stock.find(x=>x.name===item.name);if(s)s.qty=Math.max(0,s.qty-item.qty)});save();clearOrderForm();updateHeader();$('#orderNumber').textContent='#'+nextOrder;renderProducts();printOrder(order);showToast(`Pedido #${order.no} registrado e enviado a impresión`)}
function renderOrders(){const root=$('#pendingOrders');root.innerHTML=pending.length?pending.map((o,i)=>`<article class="order-card"><div class="order-card-top"><div><h3>Pedido #${o.no}</h3><p>${escapeHTML(o.customer||'Sin nombre')} · ${new Date(o.date).toLocaleTimeString('es-UY',{hour:'2-digit',minute:'2-digit'})}</p></div><span class="status-pill ${o.paymentStatus==='Pagado'?'paid':'pending'}">${o.paymentStatus}</span></div><div class="order-items">${o.items.map(x=>`<div class="order-item-line"><span>${x.qty} × ${escapeHTML(x.name)}${x.extras?.length?`<small>${x.extras.map(e=>escapeHTML(e.name)).join(' · ')}</small>`:''}</span><b>${money(x.qty*x.price)}</b></div>`).join('')}</div>${o.obs?`<p>Nota: ${escapeHTML(o.obs)}</p>`:''}<div class="total-row"><span>Total</span><strong>${money(o.total)}</strong></div><div class="order-actions">${o.paymentStatus!=='Pagado'?`<button data-order="pay" data-index="${i}">Confirmar pago</button><button class="cancel-order" data-order="cancel" data-index="${i}">Cancelar pedido</button>`:''}<button data-order="print" data-index="${i}">Imprimir</button><button class="finish" data-order="finish" data-index="${i}">Marcar como entregado</button></div></article>`).join(''):`<div class="empty-state"><div><div class="empty-icon">✓</div><p>No hay pedidos abiertos</p><small>Los nuevos pedidos aparecerán acá.</small></div></div>`;updateHeader()}
function orderAction(action,index){const o=pending[index];if(!o)return;const finish=()=>{const current=pending.findIndex(x=>x.id===o.id);if(current>=0)pending.splice(current,1);save();renderOrders();showToast(`Pedido #${o.no} entregado`)};if(action==='cancel'){if(o.paymentStatus==='Pagado')return;openConfirm({title:`Cancelar pedido #${o.no}`,message:'El pedido se eliminará y las unidades volverán al stock.',confirmText:'Sí, cancelar',onConfirm:()=>{o.items.forEach(item=>{const s=stock.find(x=>x.name===item.name);if(s)s.qty+=item.qty});const current=pending.findIndex(x=>x.id===o.id);if(current>=0)pending.splice(current,1);sales=sales.filter(s=>s.id!==o.id);save();renderOrders();renderProducts();updateHeader();showToast(`Pedido #${o.no} cancelado`)}});return}if(action==='pay'){o.paymentStatus='Pagado';const sale=sales.find(s=>s.id===o.id);if(sale)sale.paymentStatus='Pagado';save();renderOrders();return}if(action==='finish'&&o.paymentStatus!=='Pagado'){openModal(`<div class="confirm-dialog"><p class="eyebrow">PAGO PENDIENTE</p><h2>¿Entregar pedido #${o.no}?</h2><div class="modal-actions"><button id="modalSkip" class="secondary-action" type="button">Saltar pago</button><button id="modalConfirm" class="primary-action" type="button">Confirmar pago</button></div></div>`);$('#modalSkip').onclick=()=>{closeModal();finish()};$('#modalConfirm').onclick=()=>{o.paymentStatus='Pagado';const sale=sales.find(s=>s.id===o.id);if(sale)sale.paymentStatus='Pagado';closeModal();finish()};return}if(action==='finish')finish();if(action==='print')printOrder(o)}
function printOrder(o){const w=window.open('','_blank','width=400,height=700');w.document.write(`<!doctype html><html><head><title>Pedido ${o.no}</title><style>body{font:14px monospace;width:300px;margin:20px auto}.c{text-align:center}.r{display:flex;justify-content:space-between;padding:5px 0;border-bottom:1px dashed #aaa}.r small{display:block;margin-top:3px}h2{margin-bottom:4px}</style></head><body><div class="c"><h2>MALÉFICA BURGER</h2><b>Pedido #${o.no}</b><p>${new Date(o.date).toLocaleString('es-UY')}</p></div><p>${escapeHTML(o.customer||'')}<br>${o.type} · ${o.source}</p><hr>${o.items.map(x=>`<div class="r"><span>${x.qty} × ${escapeHTML(x.name)}${x.extras?.length?`<small>+${x.extras.map(e=>escapeHTML(e.name)).join(', +')}</small>`:''}</span><b>${money(x.qty*x.price)}</b></div>`).join('')}<h2>TOTAL ${money(o.total)}</h2><b>${o.paymentStatus} · ${o.paymentMethod}</b>${o.obs?`<p>Obs: ${escapeHTML(o.obs)}</p>`:''}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}
function renderStock(){const q=$('#stockSearch').value.trim().toLowerCase();const list=stock.filter(s=>s.name.toLowerCase().includes(q)&&(stockFilter==='all'||s.qty<=s.min));$('#stockList').innerHTML=list.length?list.map(s=>{const i=stock.indexOf(s),low=s.qty<=s.min;return `<div class="table-row"><span class="stock-name"><strong>${escapeHTML(s.name)}</strong><small>${products.some(p=>p.name===s.name)?'Producto del menú':'Insumo'}</small></span><input data-stock="qty" data-index="${i}" type="number" min="0" value="${s.qty}"><input data-stock="min" data-index="${i}" type="number" min="0" value="${s.min}"><span class="status-pill ${low?'low':'ok'}">${low?'REPONER':'OK'}</span></div>`}).join(''):`<div class="empty-state"><div><p>No hay resultados</p></div></div>`}
function weekStart(date){const d=new Date(date+'T12:00:00'),day=d.getDay();d.setDate(d.getDate()+(day===0?-6:1-day));return d.toISOString().slice(0,10)}
const shiftDate=(date,days)=>{const d=new Date(date+'T12:00:00');d.setDate(d.getDate()+days);return d.toISOString().slice(0,10)};
const MONTHS=['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre'];
function populatePeriodSelects(){const current=new Date().getFullYear(),years=[...new Set([...Array.from({length:6},(_,i)=>current+i),current-1,current-2,...sales.map(s=>Number(dayKey(s.date).slice(0,4))),...expenses.map(e=>Number(String(e.date).slice(0,4)))])].filter(Boolean).sort((a,b)=>b-a),monthOptions=MONTHS.map((name,i)=>`<option value="${i+1}">${name}</option>`).join(''),yearOptions=years.map(year=>`<option value="${year}">${year}</option>`).join('');['salesMonth','expenseMonth'].forEach(id=>{$('#'+id).innerHTML=monthOptions});['salesYear','expenseYear'].forEach(id=>{$('#'+id).innerHTML=yearOptions})}
function periodRange(prefix){let from=$('#'+prefix+'From')?.value,to=$('#'+prefix+'To')?.value;if(from&&to){if(from>to)[from,to]=[to,from];return{from,to}}const year=Number($('#'+prefix+'Year').value),month=Number($('#'+prefix+'Month').value),last=String(new Date(year,month,0).getDate()).padStart(2,'0');return{from:`${year}-${String(month).padStart(2,'0')}-01`,to:`${year}-${String(month).padStart(2,'0')}-${last}`}}
function renderSales(){const {from,to}=periodRange('sales'),todayActive=from===today()&&to===today();$('#salesToday').classList.toggle('active',todayActive);const inRange=(sale,start,end)=>{const date=dayKey(sale.date);return date>=start&&date<=end};const list=sales.filter(s=>inRange(s,from,to)),revenue=list.reduce((a,s)=>a+s.total,0),units=list.reduce((a,s)=>a+s.items.reduce((n,x)=>n+x.qty,0),0),avg=list.length?revenue/list.length:0,mode=$('#salesCompare').value;let previous=[],comparison='';if(mode!=='none'){let prevFrom,prevTo;if(mode==='previous'){const days=Math.round((new Date(to+'T12:00:00')-new Date(from+'T12:00:00'))/86400000)+1;prevTo=shiftDate(from,-1);prevFrom=shiftDate(prevTo,-days+1)}else{const days=mode==='week'?7:30;prevFrom=shiftDate(from,-days);prevTo=shiftDate(to,-days)}previous=sales.filter(s=>inRange(s,prevFrom,prevTo));const previousRevenue=previous.reduce((a,s)=>a+s.total,0),change=previousRevenue?Math.round((revenue-previousRevenue)/previousRevenue*100):null;comparison=`${money(previousRevenue)}${change===null?'':' · '+(change>=0?'+':'')+change+'%'}`}$('#salesStats').innerHTML=stat('Facturación',money(revenue))+stat('Pedidos',list.length)+stat('Productos vendidos',units)+stat('Ticket promedio',money(avg))+(mode!=='none'?stat('Período comparado',comparison):'');const counts={};list.forEach(s=>s.items.forEach(x=>counts[x.name]=(counts[x.name]||0)+x.qty));const rank=Object.entries(counts).sort((a,b)=>b[1]-a[1]).slice(0,8),max=rank[0]?.[1]||1;$('#topProducts').innerHTML=rank.length?rank.map(([n,c],i)=>`<div class="rank-line"><span class="rank-number">${i+1}</span><div class="rank-bar"><b>${escapeHTML(n)}</b><span style="width:${c/max*100}%"></span></div><strong>${c}</strong></div>`).join(''):'<p class="muted">No hay ventas para este período.</p>';$('#salesHistory').innerHTML=list.length?list.slice().reverse().map(s=>`<article class="history-sale ${openSaleId===s.id?'open':''}"><button class="history-sale-main" data-sale-toggle="${s.id}" aria-expanded="${openSaleId===s.id}"><span><b>Pedido #${s.no}</b><small>${new Date(s.date).toLocaleString('es-UY')} · ${escapeHTML(s.customer||s.source)}</small></span><strong>${money(s.total)}</strong><i>⌄</i></button><button class="history-sale-remove" data-sale-remove="${s.id}" aria-label="Eliminar pedido #${s.no}" title="Eliminar venta">×</button>${openSaleId===s.id?`<div class="history-sale-detail">${s.items.map(item=>`<div><span>${item.qty} × ${escapeHTML(item.name)}${item.extras?.length?` <small>(${item.extras.map(extra=>escapeHTML(extra.name)).join(' · ')})</small>`:''}</span><b>${money(item.qty*item.price)}</b></div>`).join('')}<p>${escapeHTML(s.customer||'Sin nombre')} · ${escapeHTML(s.type||'')} · ${escapeHTML(s.paymentStatus||'')}</p>${s.obs?`<p>Nota: ${escapeHTML(s.obs)}</p>`:''}</div>`:''}</article>`).join(''):'<p class="muted">No hay movimientos para este período.</p>'}
function removeSale(id){const sale=sales.find(s=>s.id===id);if(!sale)return;openConfirm({title:`Eliminar pedido #${sale.no}`,message:'Se eliminará esta venta del historial y se restaurará su stock.',confirmText:'Sí, eliminar',onConfirm:()=>{sale.items.forEach(item=>{const stockItem=stock.find(s=>s.name===item.name);if(stockItem)stockItem.qty+=item.qty});sales=sales.filter(s=>s.id!==id);pending=pending.filter(s=>s.id!==id);if(openSaleId===id)openSaleId=null;save();updateHeader();renderProducts();renderSales();showToast(`Pedido #${sale.no} eliminado`)}})}
const stat=(label,value)=>`<div class="stat-card"><small>${label}</small><strong>${value}</strong></div>`;
function renderExpenses(){const {from,to}=periodRange('expense'),inRange=(entry,start,end)=>entry.date>=start&&entry.date<=end,list=expenses.filter(e=>inRange(e,from,to)),periodSales=sales.filter(s=>{const date=dayKey(s.date);return date>=from&&date<=to}),total=list.reduce((a,e)=>a+Number(e.amount),0),revenue=periodSales.reduce((a,s)=>a+s.total,0);$('#expenseStats').innerHTML=stat('Gastos del período',money(total))+stat('Movimientos',list.length)+stat('Ventas del período',money(revenue))+stat('Neto del período',money(revenue-total));$('#expenseList').innerHTML=list.length?list.slice().sort((a,b)=>b.date.localeCompare(a.date)||b.id-a.id).map(e=>`<div class="expense-line"><span><b>${escapeHTML(e.category)}</b><small>${e.date}${e.detail?' · '+escapeHTML(e.detail):''}</small></span><span><b>${money(e.amount)}</b><button data-expense-remove="${e.id}" aria-label="Eliminar">×</button></span></div>`).join(''):'<p class="muted">No hay gastos para este período.</p>'}
function renderSettings(){$('#priceList').innerHTML=products.map((p,i)=>`<div class="product-setting-row"><span class="product-setting-name"><b>${escapeHTML(p.name)}</b><small>${escapeHTML(p.cat)}</small></span><label class="manual-price-check"><input data-manual-index="${i}" type="checkbox" ${p.manualPrice||p.price<=0?'checked':''}><span>Precio manual</span></label><input data-price-index="${i}" type="number" min="0" value="${p.price||0}" ${p.manualPrice||p.price<=0?'disabled':''} aria-label="Precio de ${escapeHTML(p.name)}"><button class="icon-danger" data-product-remove="${i}" aria-label="Eliminar ${escapeHTML(p.name)}">×</button></div>`).join('');$('#extraList').innerHTML=extras.map((x,i)=>`<div class="extra-setting-row"><span>${escapeHTML(x.name)}</span><input data-extra-price="${i}" type="number" min="0" value="${x.price}" aria-label="Precio de ${escapeHTML(x.name)}"><button class="icon-danger" data-extra-remove="${i}" aria-label="Eliminar ${escapeHTML(x.name)}">×</button></div>`).join('');$('#expenseCategoryList').innerHTML=expenseCategories.map((c,i)=>`<div class="category-setting-row"><span>${escapeHTML(c)}</span><button class="icon-danger" data-expense-category-remove="${i}" aria-label="Eliminar ${escapeHTML(c)}">×</button></div>`).join('');renderExpenseCategoryOptions()}
function renderExpenseCategoryOptions(){const select=$('#expenseCategory'),current=select.value;select.innerHTML=expenseCategories.map(x=>`<option>${escapeHTML(x)}</option>`).join('');if(expenseCategories.includes(current))select.value=current}
function openNewProductModal(){const cats=[...new Set(products.map(p=>p.cat).filter(Boolean))];openModal(`<h2>Agregar producto</h2><form id="newProductForm" class="modal-form"><label>Nombre<input id="newProductName" required autocomplete="off"></label><label>Categoría<input id="newProductCategory" list="productCategories" required autocomplete="off"><datalist id="productCategories">${cats.map(c=>`<option value="${escapeHTML(c)}">`).join('')}</datalist></label><label class="manual-price-check modal-check"><input id="newProductManual" type="checkbox"><span>Definir el precio al agregarlo al ticket</span></label><label id="newProductPriceWrap">Precio fijo<input id="newProductPrice" type="number" min="1" step="1" required></label><button class="primary-action" type="submit">Guardar producto</button></form>`);const manual=$('#newProductManual'),price=$('#newProductPrice'),wrap=$('#newProductPriceWrap');manual.onchange=()=>{price.disabled=manual.checked;price.required=!manual.checked;wrap.classList.toggle('disabled-field',manual.checked)};$('#newProductForm').onsubmit=e=>{e.preventDefault();const name=$('#newProductName').value.trim(),cat=$('#newProductCategory').value.trim(),manualPrice=manual.checked,priceValue=manualPrice?0:Number(price.value);if(!name||!cat||(!manualPrice&&priceValue<=0))return;if(products.some(p=>p.name.toLowerCase()===name.toLowerCase()))return showToast('Ya existe un producto con ese nombre');products.push({name,cat,price:priceValue,manualPrice,desc:'',emoji:'🍔'});stock.push({name,qty:20,min:5,cost:0});save();closeModal();renderSettings();renderCategories();renderProducts();showToast('Producto agregado')}}
function removeProductSetting(index){const p=products[index];if(!p)return;openConfirm({title:`Eliminar ${p.name}`,message:'Se quitará del menú y del stock. Las ventas anteriores se conservarán.',confirmText:'Sí, eliminar',onConfirm:()=>{products.splice(index,1);stock=stock.filter(s=>s.name!==p.name);cart=cart.filter(x=>x.productIndex!==index).map(x=>({...x,productIndex:x.productIndex>index?x.productIndex-1:x.productIndex}));if(activeCategory!=='Todos'&&!products.some(x=>x.cat===activeCategory))activeCategory='Todos';save();renderSettings();renderCategories();renderProducts();renderCart();showToast('Producto eliminado')}})}
function openExpenseCategoryModal(){openModal(`<h2>Agregar categoría de gasto</h2><form id="newExpenseCategoryForm" class="modal-form"><label>Nombre<input id="newExpenseCategoryName" required autocomplete="off"></label><button class="primary-action" type="submit">Guardar categoría</button></form>`);$('#newExpenseCategoryForm').onsubmit=e=>{e.preventDefault();const name=$('#newExpenseCategoryName').value.trim();if(!name)return;if(expenseCategories.some(c=>c.toLowerCase()===name.toLowerCase()))return showToast('La categoría ya existe');expenseCategories.push(name);save();closeModal();renderSettings();showToast('Categoría agregada')}}
function removeExpenseCategory(index){const name=expenseCategories[index];if(!name)return;if(expenseCategories.length===1)return showToast('Debe quedar al menos una categoría');openConfirm({title:`Eliminar ${name}`,message:'Dejará de aparecer en nuevos gastos. Los gastos anteriores se conservarán.',confirmText:'Sí, eliminar',onConfirm:()=>{expenseCategories.splice(index,1);save();renderSettings();showToast('Categoría eliminada')}})}
function openExtraModal(){openModal(`<h2>Agregar extra</h2><form id="newExtraForm" class="modal-form"><label>Nombre<input id="newExtraName" required autocomplete="off"></label><label>Precio<input id="newExtraPrice" type="number" min="0" step="1" required></label><button class="primary-action" type="submit">Guardar extra</button></form>`);$('#newExtraForm').onsubmit=e=>{e.preventDefault();const name=$('#newExtraName').value.trim(),price=Number($('#newExtraPrice').value);if(!name||price<0)return;if(extras.some(x=>x.name.toLowerCase()===name.toLowerCase()))return showToast('Ese extra ya existe');extras.push({name,price});save();closeModal();renderSettings();showToast('Extra agregado')}}
function removeExtra(index){const extra=extras[index];if(!extra)return;openConfirm({title:`Eliminar ${extra.name}`,message:'Ya no estará disponible para nuevas hamburguesas.',confirmText:'Sí, eliminar',onConfirm:()=>{extras.splice(index,1);save();renderSettings();showToast('Extra eliminado')}})}
function updateHeader(){$('#pendingBadge').textContent=pending.length;$('#pendingBadge').dataset.zero=String(!pending.length)}
function openModal(html){$('#modalBody').innerHTML=html;$('#modal').classList.add('open');$('#modal').setAttribute('aria-hidden','false');setTimeout(()=>$('#modal input, #modalConfirm')?.focus(),30)}function closeModal(){$('#modal').classList.remove('open');$('#modal').setAttribute('aria-hidden','true')}
function openConfirm({title,message,confirmText='Confirmar',onConfirm}){openModal(`<div class="confirm-dialog"><p class="eyebrow">CONFIRMACIÓN</p><h2>${escapeHTML(title)}</h2><p class="confirm-copy">${escapeHTML(message)}</p><div class="modal-actions"><button id="modalCancel" class="secondary-action" type="button">Volver</button><button id="modalConfirm" class="primary-action danger-action" type="button">${escapeHTML(confirmText)}</button></div></div>`);$('#modalCancel').onclick=closeModal;$('#modalConfirm').onclick=()=>{closeModal();onConfirm()}}
$$('[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view)));$('#categoryTabs').addEventListener('click',e=>{const b=e.target.closest('[data-cat]');if(!b)return;activeCategory=b.dataset.cat;renderCategories();renderProducts()});$('#productGrid').addEventListener('click',e=>{const action=e.target.closest('[data-product-action]');if(action){e.stopPropagation();changeProductQty(Number(action.dataset.index),action.dataset.productAction==='plus'?1:-1);return}const card=e.target.closest('[data-product]');if(card)addProduct(Number(card.dataset.product))});$('#productGrid').addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&!e.target.closest('[data-product-action]')){e.preventDefault();const card=e.target.closest('[data-product]');if(card)addProduct(Number(card.dataset.product))}});$('#productSearch').addEventListener('input',renderProducts);$('#cart').addEventListener('click',e=>{const b=e.target.closest('[data-cart-action]');if(!b)return;const i=Number(b.dataset.index),a=b.dataset.cartAction;if(a==='plus')cart[i].qty++;if(a==='minus'){cart[i].qty--;if(cart[i].qty<=0)cart.splice(i,1)}if(a==='remove')cart.splice(i,1);renderCart()});$('#clearCart').onclick=clearOrderForm;$('#saveOrder').onclick=registerOrder;$$('[data-note]').forEach(b=>b.onclick=()=>{$('#notes').value+=($('#notes').value?', ':'')+b.dataset.note});$('#pendingOrders').addEventListener('click',e=>{const b=e.target.closest('[data-order]');if(b)orderAction(b.dataset.order,Number(b.dataset.index))});$('#stockSearch').addEventListener('input',renderStock);$('#stockStatusFilters').addEventListener('click',e=>{const b=e.target.closest('[data-status]');if(!b)return;stockFilter=b.dataset.status;$$('#stockStatusFilters button').forEach(x=>x.classList.toggle('active',x===b));renderStock()});$('#stockList').addEventListener('change',e=>{const i=Number(e.target.dataset.index),field=e.target.dataset.stock;if(field){stock[i][field]=Math.max(0,Number(e.target.value));save();renderStock();renderProducts()}});$('#addStock').onclick=()=>openModal(`<h2>Agregar insumo</h2><form id="stockModalForm" class="modal-form"><label>Nombre<input id="newStockName" required></label><label>Cantidad inicial<input id="newStockQty" type="number" min="0" value="0" required></label><label>Stock mínimo<input id="newStockMin" type="number" min="0" value="5" required></label><button class="primary-action" type="submit">Agregar al stock</button></form>`);$('#modalBody').addEventListener('submit',e=>{if(e.target.id!=='stockModalForm')return;e.preventDefault();const name=$('#newStockName').value.trim();if(!name)return;stock.push({name,qty:Number($('#newStockQty').value),min:Number($('#newStockMin').value),cost:0});save();closeModal();renderStock();showToast('Insumo agregado')});$$('[data-close-modal]').forEach(x=>x.onclick=closeModal);document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});$('#salesDate').addEventListener('change',renderSales);$('#expenseForm').onsubmit=e=>{e.preventDefault();const amount=Number($('#expenseAmount').value);if(amount<=0)return;expenses.push({id:Date.now(),date:$('#expenseDate').value||today(),category:$('#expenseCategory').value,amount,detail:$('#expenseDetail').value.trim()});save();e.target.reset();$('#expenseDate').value=today();renderExpenses();showToast('Gasto guardado')};$('#expenseList').addEventListener('click',e=>{const b=e.target.closest('[data-expense-remove]');if(!b)return;expenses=expenses.filter(x=>x.id!==Number(b.dataset.expenseRemove));save();renderExpenses()});$('#priceList').addEventListener('change',e=>{const i=Number(e.target.dataset.priceIndex);if(Number.isNaN(i))return;products[i].price=Math.max(0,Number(e.target.value));save();renderProducts();showToast('Precio actualizado')});$('#exportData').onclick=()=>{const blob=new Blob([JSON.stringify({version:2,exportedAt:new Date().toISOString(),products,stock,sales,pending,expenses,nextOrder},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`malefica-backup-${today()}.json`;a.click();URL.revokeObjectURL(a.href)};$('#importData').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const d=JSON.parse(await f.text());products=d.products||products;stock=d.stock||stock;sales=d.sales||sales;pending=d.pending||pending;expenses=d.expenses||expenses;nextOrder=Number(d.nextOrder||nextOrder);save();location.reload()}catch{showToast('No se pudo importar el archivo')}};$('#resetData').onclick=()=>openModal(`<h2>¿Borrar los movimientos?</h2><p class="muted">Se eliminarán ventas, pedidos y gastos. El menú y el stock se mantendrán.</p><button id="confirmReset" class="primary-action">Sí, borrar movimientos</button>`);$('#modalBody').addEventListener('click',e=>{if(e.target.id==='confirmReset'){sales=[];pending=[];expenses=[];nextOrder=1;save();closeModal();updateHeader();showToast('Movimientos eliminados')}});
$('#priceList').addEventListener('change',e=>{if(!e.target.matches('[data-manual-index]'))return;const i=Number(e.target.dataset.manualIndex);products[i].manualPrice=e.target.checked;if(!e.target.checked&&products[i].price<=0)products[i].price=1;save();renderSettings();renderProducts();showToast(e.target.checked?'Precio manual activado':'Precio fijo activado')});$('#priceList').addEventListener('click',e=>{const b=e.target.closest('[data-product-remove]');if(b)removeProductSetting(Number(b.dataset.productRemove))});$('#addProductSetting').onclick=openNewProductModal;$('#addExpenseCategory').onclick=openExpenseCategoryModal;$('#expenseCategoryList').addEventListener('click',e=>{const b=e.target.closest('[data-expense-category-remove]');if(b)removeExpenseCategory(Number(b.dataset.expenseCategoryRemove))});
$('#productGrid').addEventListener('click',e=>{const b=e.target.closest('[data-product-extra]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();openBurgerExtras(Number(b.dataset.productExtra))},true);
$('#addExtraSetting').onclick=openExtraModal;$('#extraList').addEventListener('change',e=>{if(!e.target.matches('[data-extra-price]'))return;const i=Number(e.target.dataset.extraPrice);extras[i].price=Math.max(0,Number(e.target.value));save();showToast('Precio del extra actualizado')});$('#extraList').addEventListener('click',e=>{const b=e.target.closest('[data-extra-remove]');if(b)removeExtra(Number(b.dataset.extraRemove))});
$('#exportData').onclick=()=>{const blob=new Blob([JSON.stringify({version:4,exportedAt:new Date().toISOString(),products,stock,sales,pending,expenses,expenseCategories,extras,nextOrder},null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`malefica-backup-${today()}.json`;a.click();URL.revokeObjectURL(a.href)};$('#importData').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{const d=JSON.parse(await f.text());products=d.products||products;stock=d.stock||stock;sales=d.sales||sales;pending=d.pending||pending;expenses=d.expenses||expenses;expenseCategories=d.expenseCategories||expenseCategories;extras=d.extras||extras;nextOrder=Number(d.nextOrder||nextOrder);save();location.reload()}catch{showToast('No se pudo importar el archivo')}};
const applyTicketTheme=light=>{$('.ticket-panel').classList.toggle('ticket-light',light);$('#ticketThemeToggle').checked=light;localStorage.setItem('master_ticket_theme',light?'light':'dark')};
$('#salesHistory').addEventListener('click',e=>{const remove=e.target.closest('[data-sale-remove]');if(remove)return removeSale(Number(remove.dataset.saleRemove));const toggle=e.target.closest('[data-sale-toggle]');if(toggle){const id=Number(toggle.dataset.saleToggle);openSaleId=openSaleId===id?null:id;renderSales()}});
normalizeStock();renderCategories();renderProducts();renderCart();updateHeader();$('#orderNumber').textContent='#'+nextOrder;populatePeriodSelects();const periodNow=new Date();['salesMonth','expenseMonth'].forEach(id=>$('#'+id).value=String(periodNow.getMonth()+1));['salesYear','expenseYear'].forEach(id=>$('#'+id).value=String(periodNow.getFullYear()));$('#salesDate').value=today();$('#expenseDate').value=today();renderExpenseCategoryOptions();$$('#salesFrom,#salesTo,#salesCompare').forEach(el=>el.addEventListener('change',renderSales));$('#salesToday').addEventListener('click',()=>{$('#salesFrom').value=today();$('#salesTo').value=today();renderSales()});$$('#expenseFrom,#expenseTo').forEach(el=>el.addEventListener('change',renderExpenses));$$('#salesMonth,#salesYear').forEach(el=>el.addEventListener('change',()=>{$('#salesFrom').value='';$('#salesTo').value='';renderSales()}));$$('#expenseMonth,#expenseYear').forEach(el=>el.addEventListener('change',()=>{$('#expenseFrom').value='';$('#expenseTo').value='';renderExpenses()}));applyTicketTheme(localStorage.getItem('master_ticket_theme')==='light');$('#ticketThemeToggle').addEventListener('change',e=>applyTicketTheme(e.target.checked));

// Structured actions for browsers and assistants that support WebMCP.
if(document.modelContext?.registerTool){
  const webTools=new AbortController();
  const register=tool=>Promise.resolve(document.modelContext.registerTool(tool,{signal:webTools.signal})).catch(()=>{});
  register({name:'list_menu_products',title:'List menu products',description:'List the products currently available in the Maléfica Burger menu, with price and stock.',inputSchema:{type:'object',properties:{category:{type:'string'}},additionalProperties:false},annotations:{readOnlyHint:true,untrustedContentHint:false},execute:({category}={})=>products.filter(p=>!category||p.cat===category).map(p=>({name:p.name,category:p.cat,price:p.price,stock:stock.find(s=>s.name===p.name)?.qty??null}))});
  register({name:'add_item_to_ticket',title:'Add item to ticket',description:'Add a menu product with a fixed price to the current visible ticket.',inputSchema:{type:'object',properties:{name:{type:'string'},quantity:{type:'integer',minimum:1,maximum:50}},required:['name'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},execute:({name,quantity=1})=>{const index=products.findIndex(p=>p.name.toLowerCase()===String(name).toLowerCase());if(index<0)throw new Error('Product not found');if(products[index].price<=0)throw new Error('This product needs a price before it can be added');for(let i=0;i<quantity;i++)addProduct(index);return {added:products[index].name,quantity,ticketTotal:cartTotal()}}});
  register({name:'complete_current_order',title:'Complete current order',description:'Register the current visible ticket as a new order after customer and payment fields have been set in the interface.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},execute:()=>{if(!cart.length)throw new Error('The ticket is empty');const orderNo=nextOrder;registerOrder();return {orderNumber:orderNo,status:'registered'}}});
}
